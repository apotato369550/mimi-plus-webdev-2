# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=                # Leave blank for XAMPP default, or enter your MySQL password
DB_DATABASE=mimi_plus       # Name of your database
DB_PORT=3306

# JWT Secret (change this to a random string in production!)
JWT_SECRET=change_this_to_a_random_secret_key_for_production

# Email Configuration (for verification emails and password reset)
# For Gmail: Use an App Password (https://support.google.com/accounts/answer/185833)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_specific_password

# Server Port (optional, defaults to 5002)
PORT=5002
